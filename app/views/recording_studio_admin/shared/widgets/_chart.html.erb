<% chart_options = (widget.chart_options || {}).deep_merge(grid: { show: false }) %>

<%= render FlatPack::Chart::Component.new(
  type: widget.chart_type,
  series: widget.series,
  options: chart_options,
  card: false
) %>